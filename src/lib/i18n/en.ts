import { es, type Dictionary } from "./es";

// Phase 1: Spanish only (per brief: "no language toggle").
// English deferred to Phase 2. Stub returns es for type compatibility.
export const en: Dictionary = es;